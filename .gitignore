# ============================================================
# 자격증명 — 절대 커밋 금지 (MIG.env / .env / PG.env 등)
# 템플릿은 *.env.example 로 두면 추적된다.
# ============================================================
*.env
!*.env.example

# ============================================================
# 오프라인 설치용 wheel / 로컬 venv (용량이 커 저장소에 넣지 않는다)
# ============================================================
wheels/
.venv/
venv/
__pycache__/
*.pyc

# ============================================================
# 실행 산출물 — 스크립트가 만들어내는 것들 (레포 공통 규약: ${BASE_PATH}/log, tmp)
# ============================================================
log/
tmp/
*.log
# TIBERO/start.sh 가 기록하는 PID 파일
sqlgrid.pid
nohup.out

# ============================================================
# 배포 패키지 (TIBERO/package.sh 산출물 — 1개당 108M)
# ============================================================
dist/
*.tar.gz
*.tgz
*.sha256

# ============================================================
# 대용량 바이너리 / 데이터 (Git 으로 관리할 대상이 아니다)
# ============================================================
TIBERO/tbcheck
*.dmp
*.dmp.gz

# ============================================================
# 편집기 / OS 부산물
# ============================================================
*.swp
*.swo
*~
.DS_Store

# ============================================================
# 로컬 전용 설정 (머신마다 다르므로 공유하지 않는다)
# ============================================================
.claude/settings.local.json
