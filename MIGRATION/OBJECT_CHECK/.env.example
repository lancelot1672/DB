# OBJECT_CHECK v1 environment
# Copy to .env  (cp .env.example .env)  and fill in.
# .env is gitignored (repo .gitignore = *.env). Never hardcode credentials in scripts.

DB_USER=dbadm
DB_PASS=changeme

# Working base path. log/ and tmp/ are created under here automatically.
BASE_PATH=/home/oracle/object_check
